.remove-app{
    background: rgba(0,0,0,.8);
    height: 100%;
    width: 100%;
    position: relative;
    z-index: 1;
    .delete-panel{
        height: 20%;
        background: url('images/delete-panel.png') no-repeat center;
        background-size: toRem(37) toRem(40)
        opacity: .5;
        &.active{
            opacity: 1;
        }
    }
}